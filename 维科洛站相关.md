# GVY 维科洛交易查询站：接手交接文档

## 2026-09-21 NAS 地点汉化与同 LIVE 校准（发布验收中）

- 本节为最新规则，覆盖下方“文稿/data 为源”“只有新 LIVE 才刷新”的历史描述。用户授权修复漏译并部署，后续明确不新增周审/独立命令，只在原数据刷新时校准；另由主站协调任务负责 NAS→GitHub 输入同步，本仓库不部署 NAS 同步器或新增认证。
- 修改前基线 `98eb945a33bb429a5c235a9f788201f0f48d796f`，工作区干净；回退标签 `backup-before-nas-localization-20260921`。只改维科洛仓库数据、汉化/刷新/审计及测试，不改 UI 样式、其他站、DNS 或 EdgeOne 设置。
- 权威只读源 `nas:/volume1/docker/starcitizen-shared-input/localization/data/localization/chinese_(simplified)/global.ini`；2026-09-17 18:54:40 +08，10,664,866 字节，SHA-256 `2b8be0f1f56d332b707d87453767c2176c16dfc4693885ce94239648600daded`。`Frontend_PU_Version=4.10: 奥里森之围` 仅证明 4.10 系列，与当前 `4.10.1 LIVE.12660092` 系列匹配，不能证明精确 build。
- 原因：获取地点用手工整串替换，未覆盖新的商店标签、缩写、星门父星系和前哨名称；旧派生快照不收录地点键，旧残留审计只检查固定词表，初次全站扫描有 29 个物品/67 条去重商店地点组合漏译却报告 0 项。NAS 与旧本机 INI 均已有截图所需词条，并非源文件缺词。
- 截图修正：Bullock's Reach→犍牛原，Kinder Plots→容善镇，The Yard→“庭院”，Shop Terminal→商店终端；埃弗勒斯空间站、泰拉星门、护甲店/货运服务等一并按官方键处理。新增 `official-location-source.mjs` 保存官方键及已核验终端/地点 ID。PSSA/PSSD/PSSL/PSST 经上游终端 833/834/842/838 核对后对应人民服务空间站 阿尔法/德尔塔/拉姆达/西塔，不靠缩写猜测。
- 两个任务跟随 NAS 官方 key 校正为“快帆现在主打战斗”和“太多枪了”；271 个物品名不变。候选总计修改 2 个交易和 72 个物品（地点/获取说明及关联任务名/来源元数据），无交易或物品增删。66 个物品 acquisition 有差异，其中 67 处 location 和 1 处 label；包含全量中文排序后前 8 个展示地点调整及恢复原说明换行/空格。价格、数量、奖励、配方和图片均未改变。265 个配方材料名已通过官方中文覆盖审计。
- 保留待确认英文：Green Circle（绿环公寓/大厅无法唯一定位）和 Sharp Shooters（不能误套 Skutters 的“射手”译名）。仅离线 `data/audits/content-audit.json` 记录，不在网页添加维护文字。SCU/ASD/QV/PAF/OLP/RCMBNT、罗马数字与 L1 等编号不硬翻译。Starfarer 的“星际远航者”原样保留。
- 本机 `refresh:data` / `refresh:data:publish-check` 直接读 NAS 并核验哈希；GitHub 从自己仓库 `nas-localization` 分支锁定 commit，读取同 commit 的 `source.json`、`global.ini.gz`，验证 schema/长度/哈希/versionLabel/系列，不执行分支代码。失败保留原稳定数据，无静默旧缓存回退。10MB 原始 INI 不入 main/dist，原派生快照路径不变。
- 原 6 小时轻量检查增加同步源哈希比较，输出锁定 commit；同 LIVE 汉化变化也走原完整刷新。未新建 schedule/周审/命令。无语义变化时不写数据、不构建、不提交；Actions cache 仅记录成功审查的输入组合（源哈希+LIVE+脚本/派生快照版本），丢失后可安全重查，不能跳过新版本或新规则的检查。
- 已完成本地 74 项测试、数据审计、Schema、构建；86 笔交易、271 物品、118/118 上交材料图片与获取说明完整。相同源连续完整刷新第二次 `changed: [] / unchanged: true`。尚待本节后续追加 PR、生产部署与 NAS 输入分支端到端验证结果，不能把本条当成已经上线。
- 浏览器本地实测 390×844、2560×1440 的草地故夕食草兽蛋获取弹窗：四处商店均为中文，页面 scrollWidth 等于视口宽，控制台 0 error / 0 warning；截图 `output/playwright/location-local-390.png`、`location-local-2560.png`。构建 JS 为 `index-CB2w_cyk.js`，CSS 仍为 `index-DnmXUqCl.css`。严格字段比较已验证所有交易事实（除两处任务名/抓取时间）、物品名/描述/配方/图片/价格/获取来源 URL 均与发布前一致。

## 2026-09-20 新星远 Logo 同步

- 已正式发布：[PR #1](https://github.com/B1ndoX/gvy-wikelo-site/pull/1) 正常合并，功能提交 `53f0495770960627cb8735b9fd6ce52bb152dc96`，生产合并提交 `70a42a53c99b4ea0ff538f58b395c602e813e405`。2026-09-20 15:40 正式域名返回 `assets/index-BCu8jyYf.js` 与 `assets/index-DnmXUqCl.css`。5 个线上品牌资源均 HTTP 200、SHA-256 与统一 manifest 完全一致；HTML 中 4 个图标引用为 `v=17f28ec0f59c`，页脚为 `v=f2df3e742db8`。
- 生产浏览器验证 320/390/1440/1920/2560px：Logo 自然尺寸1254、显示34×34，计算变换均为 `matrix(1,0,0,1,0,-1)`；无页面横向溢出，控制台0错误/警告，维科洛头像仍为 `/images/wikelo.webp`。截图 `output/playwright/logo-production-20260920-390.png`、`logo-production-20260920-1440.png`。HTTP→HTTPS 301；本次数据、刷新脚本、工作流无差异。发布前63项测试、内容审计、Schema、构建及同LIVE轻量检查通过。后续仅文档提交补记本验收，不改变产品资源。

- 最新发布授权：用户已明确要求“各自部署”，本节后续的“仅本地、未部署”均为验收阶段历史状态。本次按 PR 合并发布已验收 Logo、版本化图标和最终 `translateY(-1px)`；不恢复 -3px，不混入数据刷新。发布基线 `639cde5`，远端回退标签 `backup-before-xingyuan-logo-20260920`，完整旧资源另有下述归档。发布前已 fetch 确认远端一致，仅本节列明的9个文件有修改。

- 最新对齐验收：用户认为上移3px过多，已将原图片规则调整为 `translateY(-1px)`，相对上一版下降2px，以此为最终本地数值。390/1440px 确认变换为 -1px、图片34×34、无溢出；构建通过，4175预览及两张截图已更新。此前 -3px 是历史试调值，勿恢复。

- 用户通过主站任务指定 `/Users/bindox/Downloads/星远_1254_裁剪缩放轻锐化版.png`，要求本站同步所有舰队品牌图与标签图标。本轮明确只做本地，不提交、推送或部署；此前署名的部署授权不适用于本次 Logo。
- 基线 `639cde58177c319c5919fb4b7cad48766a8cd53d`，fetch 后工作区干净且与远端同步。替换前备份 `/Users/bindox/Documents/Codex/Backups/gvy-wikelo-logo-20260920.htlI0D/before.tar`，SHA-256 `a27c6e50a9bf2b05a6dd5f0367550edc9f6bab7d806e420870791cbee405e90d`。
- 统一资源位于 `/Users/bindox/Documents/Codex/Previews/gvy-logo-compare-20260920/adopted-assets/`，完整哈希见该目录 manifest.json。使用其中 `gvy-logo-hq.png` 覆盖 `public/assets/gvy-logo.png`，1254×1254、1,145,403 字节，SHA-256 `f2df3e742db8147c26ab62e5d18cade71dc1250c5f776061cccab73b0217eef9`；统一制作流程已确认主图与选定源 RGBA 像素一致，本项目不再次裁剪或锐化。页脚继续显示 34×34px，CSS 不改，版本为 `?v=f2df3e742db8`。
- 同步替换 32/192px PNG、180px Apple Touch 和根 favicon.ico，统一 `?v=17f28ec0f59c`；index.html 增加显式带版本的 ICO 引用并更新原三个 PNG 引用。没有独立 manifest、404 或分享图入口；维科洛角色头像 `/images/wikelo.webp` 保持不变。
- 本地构建、63 项测试、数据审计、Schema 校验通过；5 个实际资源 URL 返回 200 且响应哈希匹配统一清单。320/390/1440/1920/2560px 页脚 Logo 均加载成功、34×34px、无页面横向溢出，控制台无错误。预览 `http://localhost:4175/`；截图在 `output/playwright/logo-20260920-390.png` 和 `output/playwright/logo-20260920-1440.png`。业务数据、CSS、自动刷新和云端配置未改。
- 后续用户要求左下角 Logo 与旁边文字视觉对齐：在 `blueprint-theme.css` 原 `.footer-brand > img` 规则中增加 `transform: translateY(-3px)`（无已有变换冲突），桌面/手机统一只上移图片。390/1440/2560px 实测位移恰为 -3px、图仍为34×34、文字坐标未变、无横向溢出；构建与 diff 检查通过，预览截图已更新。此追加只改一条 CSS，不增加 JS，仍未提交/推送/部署；上条“CSS 未改”为追加前记录。

## 2026-09-20 页头与交易详情制作署名

- 按用户提供的蓝图站截图，在维科洛页头副标题下方新增独立小字“制作：Ayuan”，使用低调青色；维科洛此前没有 `by: A Yuan` 行，本站新增署名直接使用用户指定的中文格式。
- 本轮基于干净工作区 fetch 并快进到 `26f9370`，保留远端两次自动数据更新。只修改 `src/App.tsx`、`src/blueprint-theme.css` 与本文；修改前副本保存在 `/tmp/gvy-wikelo-author-backup.DI1iqi/`。
- 构建及现有 63 项测试通过；浏览器检查 320、390、768、1440、1920、2560px 下署名可见、位于页头内且无页面横向溢出，控制台无错误。保留本地预览 `http://localhost:4175/`；本轮尚未提交、推送或部署。
- 用户追加要求在交易详情顶部区域右下角、分隔线上方同样显示小号“制作：Ayuan”。已加入全部交易详情，保留专用底部间距防止与标题重叠；320/390/1920/2560px 实测右对齐、无遮挡或弹窗横向溢出，追加修改后构建通过。仍为本地预览。
- 用户随后明确要求“部署”，以上仅本地状态为发布前记录。发布前再次 fetch，基线仍为 `26f9370`；回退标签 `backup-before-ayuan-credit-20260920`。本轮仅发布两处署名与样式、本文；沿用视觉发布流程，轻量 LIVE 检查为 `4.10.1 LIVE.12660092` 且未变化，数据审计与 Schema 通过（86/271/118），63 项测试及生产构建通过。生产目标资源为 `index-C2poOEcG.js`、`index-CfskUXvJ.css`，发布后须核对正式域名和两处署名。
- 正式功能提交 `7976dc6` 已推送并由 EdgeOne 发布。2026-09-20 15:07 正式域名浏览器已确认页头与“造一个改款天蝎座”交易详情均显示“制作：Ayuan”；线上 JS 与本地构建 SHA-256 一致：`bddb02bbc3931b96c203fca34c2c0a97f9d9d286e0a12ef0f142108dec45152d`。数据和刷新逻辑未改动。

## 2026-09-07 发布授权：新 Logo 与统一标签图标

- 用户在预览确认后明确要求“部署”。下方“仅本地、未部署、禁止提交”的段落是本轮授权之前的历史记录，不再作为阻止正常发布的要求。
- 发布批次 `20260907-brand-icons`：仅提交预览过的 Logo、页脚间距、favicon/Apple Touch/ICO 与本文；业务源码行为、数据、自动刷新、依赖/Node、云配置不变。基线 `4ba00b5`；回滚标签 `backup-before-brand-icons-20260907`。
- 发布前重新 fetch，未发现新的远端提交；远端 LIVE 轻量检查与本地均为 `4.10.0 LIVE.12519617`，63 项测试、数据审计/Schema、构建通过。本次只发品牌资源，不运行会改写数据的 `refresh:data:publish-check` 完整刷新器；以同 LIVE 检查及已有稳定数据完整校验作为本轮品牌发布门禁，禁止顺带混入其它数据更新。将来数据发布仍遵循完整刷新门禁。
- 页面 Logo 按用户确认的原像素 PNG 发布（约 823 KiB），不临时重绘/有损压缩；标签图标为独立 32/192/180px 小资源。轻量化整张 Logo 尚未实施，不能宣称已优化公网首开体积。
- 发布前文件快照与最终发布/线上验收记录在 `/Users/bindox/Documents/Codex/Backups/gvy-brand-release-20260907.5dY3Os/`；最终结果须读取该目录 release-result.md 并核对远端 main 和正式域名，不把本段授权/门禁通过当成部署完成。不要用旧的本地文件哈希恢复器覆盖发布后的提交；线上回滚使用普通 revert。

## 2026-09-07 追加：标签图标补齐与放大（仅本地）

- 用户本轮明确将三个站标签图标统一，因此 index.html 不再以 /images/wikelo.webp 作为 favicon，而改用新增 public/assets/favicon-32.png、favicon-192.png、apple-touch-icon.png，并提供 public/favicon.ico。页面中的维科洛头像仍保留，不删除图片或改变交易功能。
- 三站图标源仍为已选定的 112233.png，仅图标导出版裁出 left=60、top=9、1130×1130 区域，再等比缩至 32/192/180px；保留主要图案及文字，去掉周边透明留白/极低透明残留，视觉约放大 11%。页面 Logo 原文件、尺寸和布局不动。新图标共同版本为 `?v=29b0f33580f8`，不得沿用之前 `29a1582e730a` 的图标 URL（页面 Logo 仍使用该旧版本，二者不同）。
- 三站构建、diff 检查通过；九条本地图标 URL 全部 HTTP 200，三个站对应尺寸文件哈希一致；两个分站 ICO 容器有效。蓝图 47 项测试通过，主站 check:dist 通过。Browser 核查蓝图页面及模式切换；本次不是全站重新审计，也未验证实体手机书签图标刷新。
- 备份 `/Users/bindox/Documents/Codex/Backups/gvy-favicons-20260907.uyuC8h/` 保存本轮前图标/HTML及文档，恢复顺序见 README；此前恢复器遇到后续文件改变应拒绝，不能改期望哈希绕过。没有提交、推送、部署；以下较早章节中“工具图标未改”是追加前状态，已由本节用户新授权取代。
- 防漏项：以后更新品牌必须清点页面 Logo、rel=icon 各尺寸、Apple Touch、manifest、根 favicon.ico、404 与复用图片的分享元数据；检查实际产物和真实 URL，不把漏替换误判为浏览器缓存。不要添加刷新定时器或业务运行时代码处理图标。


## 2026-09-07 本地 Logo 预览（未提交、未部署）

- 用户本轮明确授权同步星远新 Logo，但仍要求先本地预览。本节优先于下方“交付保持干净/同步”的一般要求：保留本轮未提交更改，不清理、不推送、不部署。
- 基线 HEAD `4ba00b5`。只修改 `public/assets/gvy-logo.png`、`src/App.tsx` 页脚 img 和 `src/blueprint-theme.css` 原有 footer-brand 声明，另更新本文。没有更改交易数据、采集、自动更新、Node、依赖、路由或云端配置。
- 唯一素材 `/Users/bindox/Downloads/112233.png`，1254×1254 透明 PNG、842,508 字节，SHA-256 `29a1582e730ae698184d2748a573b4946bcd73fd4bac81132ea1e3d9feb961af`；原图逐字节复制。页脚 Logo 为 34×34px、图文间距 6px，URL `/assets/gvy-logo.png?v=29a1582e730a`。正式名保持“星远”，跨站口语仍为“星远舰队”。不替换维科洛头像或工具图标。
- 样式权威是现有 `blueprint-theme.css`，直接改原声明；不要在 styles.css 或文件末尾另叠第三套覆盖，也不要将站点数据/业务与其它项目合并。
- 验证：63 项单测、audit:data、lint、TypeScript/Vite 构建通过；86 笔交易/271 物品/118 材料数据审计通过。320/390/1440/1920/2560/3440px 无横向溢出，Logo 正常加载、尺寸/间距正确，浏览器未见控制台错误。构建原有 JS 超过 500kB 提醒仍存在（约 1,001kB 原始、176kB gzip），不因 Logo 修改引入，也没有为消除提醒更改业务打包。
- 本地预览 `http://127.0.0.1:8003/`（npm run build 后静态服务根为 dist）。截图 `/Users/bindox/Documents/Codex/Previews/xingyuan-logo-20260907/wikelo-synced-footer-1440.png`、`wikelo-synced-footer-390.png`。
- 备份 `/Users/bindox/Documents/Codex/Backups/gvy-brand-lifecycle-20260907.pBBqqU/wikelo-before.tar` 包含源码/图片/本文旧版本；按同目录 README 先校验再恢复，不能覆盖后来编辑。正式部署前重新 fetch 防止覆盖自动数据提交。
- 原图约 823 KiB，先确认视觉，正式部署前再确认保真轻量资源；实体 iPhone Safari 未实测。不要把历史 Logo、圆形候选版本或旧截图当成最新指定素材。

> 本文件是 `gvy-wikelo-site` 的正式接手交接文档。下一名完全没有聊天上下文的 AI 必须先完整阅读本文，再检查仓库状态和开始工作。项目实施变化后，应同步更新这里的架构、数据来源、产品规则、测试结果、部署状态和遗留问题，不能只记录临时方案。

本文档只属于 `gvy-wikelo-site`，不得混入蓝图查询站或舰队官网的规则与部署。

## 接手必读

- 唯一项目目录：`/Users/bindox/Documents/Codex/Projects/starcitizen-crawler/gvy-wikelo-site`
- GitHub：`B1ndoX/gvy-wikelo-site`，生产分支 `main`
- 正式域名：`https://wikelo.gvyvoyagers.vip`
- 舰队正式名称为“星远”：Logo/页脚正式主品牌使用“星远”，指向官网的跨站对外入口使用口语称呼“星远舰队”；英文 `GVY / GALACTIC VOYAGERS` 保持不变
- EdgeOne Makers 项目：`gvy-wikelo-site`
- 主交易来源：Dumper's Repo 维科洛页面；其余来源严格按下方来源—字段矩阵承担交叉校验、汉化、图片或获取方式补全职责
- 版本策略：只保存和发布数据来源实际提供的精确 LIVE 数据，不展示、不保存 PTU/EPTU
- 自动更新：主任务每 6 小时第 23 分钟轻量检查 LIVE build；独立看门狗在错峰时间检查主任务是否超过 8 小时未成功并自动补发；没有新 LIVE 就结束，不下载完整交易 bundle、不构建、不提交、不触发正式部署
- 当前稳定数据：`4.10.0 LIVE.12519617`，86 笔交易、271 个物品条目
- 当前 4.10 数据提交：`86efca0`；无人值守自动化修复提交：`e5f8433`、`2f71662`；4.10 完整刷新成功实跑：GitHub Actions `33048577364`；看门狗成功实跑：`33048848626`
- 接手后的第一项检查：`git status --short --branch`；任何修改和发布前必须先 `git fetch origin main`，防止覆盖远端自动数据更新
- 完整门禁：`npm run check:live-version`、`npm run audit:data`、`npm run lint:data`、`npm test`、`npm run build`
- 正式发布只走 EdgeOne 的仓库连接；禁止启用 GitHub Pages，禁止修改其他项目 DNS
- 交付时必须保证本地与 `origin/main` 同步且工作区干净；发现已有未提交内容时先确认归属，不得覆盖用户改动

## 用户体验定义

- 可见界面与 GVY 蓝图查询站使用同一套设计令牌和组件语言：深色玻璃壳体、青色信息、金色重点、统一圆角/边框/面板层级和响应式节奏；只统一外观，不耦合两个工具的数据、状态或抓取实现。
- 桌面端：保持维科洛适用的紧凑交易卡片网格，按视口显示 3 / 2 / 1 列；手机端：单列卡片 + 视口居中的交易详情弹窗。
- 每笔合同显示真实奖励图（有可靠图片时）、中英文名、全部所需材料和游戏版本；不得用“还有 N 项”隐藏卡片材料。
- 详情显示全部上交物、每项数量与单位、全部奖励、地点、声望、适用版本和校验状态。
- 上交物与奖励均为可点击副链接；弹窗显示实图、获取方式，以及使用或奖励该物品的相关合同与精确数量。
- 普通查询界面不得显示来源链接、校验状态、抓取术语、内部任务占位名或数据源版本；来源 URL、冲突和校验状态只留在离线数据与维护文档中。
- 对外链接白名单为星远舰队官网、蓝图站和页脚法定备案查询页；不得从图片、获取方式或配方区跳转到第三方资料站。
- 不提供库存录入、缺口判断、完成状态或账号功能；用户打开页面即可查询，站点不上传个人数据。
- 页脚 Logo 正式名显示“星远”，保留英文 `GALACTIC VOYAGERS / GVY`、非商业非官方网站声明、ICP备案和公安备案链接及图标；页头官网入口仍显示对外称呼“星远舰队”。
- 顶部不显示“数据说明”细条；数据版本、来源与刷新细节只保存在数据文件和本文档，避免干扰主查询流程。

## 来源—字段矩阵

| 来源 | 主要字段 | 当前读取方式 | 校验角色 | 限制与处理 |
|---|---|---|---|---|
| [Star Citizen Wiki：Wikelo](https://starcitizen.tools/Wikelo) | 维科洛背景、合同操作方式、三座交易站、声望级别、合同奖励实图 | 普通浏览器人工核验快照；公开页面已呈现资源的本地导入 | 背景、地点、声望、奖励图交叉核验 | 媒体端拒绝脚本请求时不绕过；逐条合同与交易站没有可靠绑定，显示“暂无” |
| [Star Citizen Wiki：Wikelo Emporium](https://starcitizen.tools/Wikelo_Emporium) | Dasi、Selo、Kinga 三座交易站及其行星系 | 人工核验快照 | 交易站列表二次校验 | 只证明总体站点，不能推断每条合同位置 |
| [Dumper's Repo](https://dumpers-repo.com/wikelo/) | 精确 LIVE 版本、当前 86 笔交易、分类、上交物、数量、SCU、奖励、蓝图池、声望增量、最低声望、有效状态 | 公开页面 bundle 内带稳定 `_source` 语义标签的数据字面量；Acorn AST 安全解析，不依赖会随构建变化的压缩变量名 | 当前 4.10.0 主结构化来源 | 不执行远端代码；发布时若缓存/部分失败则阻止 |
| [Wikelo Trades](https://wikelotrades.com/how-it-works) | 交互方式、旧版本合同需求与奖励 | 公开 manifest 与最新启用 patch 数据字面量 | 交互参考、4.8.1 二次校验 | 当前次级数据为 4.8.1，不能覆盖 4.10.0；差异保留为冲突 |
| [Star Citizen Wiki 公共 API](https://api.star-citizen.wiki) | 物品中英文资料、实图、描述、游戏版本、可拾取/制作、商店/价格 | 无需登录的公开 JSON API | 物品详情、获取方式与图片补全 | 缺失即显示“暂无”，不能根据常识补造 |
| [SC Market 公共 API](https://api.sc-market.space/docs) | 当前游戏资料版本、物品记录、蓝图与制作材料 | 读取无需登录的 `/api/v2/game-data/wiki/*` | 版本、物品与配方二次校验 | 玩家市场价格不用于固定维科洛兑换；当前公开 API 未给所有物品提供图片 |
| [UEX Marketplace](https://uexcorp.space/marketplace/) | 玩家市场物品与公开搜索索引 | 仅人工二次核对 | 名称与市场存在性参考 | robots.txt 禁止 GPTBot，详情页受 Cloudflare 保护；不自动抓取、不绕过 WAF，不用玩家价格推断配方 |
| [RSI Community Hub](https://robertsspaceindustries.com/community-hub) 与公开玩家社区 | 能明确对应维科洛特别版物品的游戏内实拍 | 普通公开页面人工核验，导入页面已经呈现的图片 | 仅用于缺图物品的视觉补充 | 必须标为“玩家社区实拍”并记录原帖；不得用来推断合同数量、版本、地点或获取方式；登录、验证码或 WAF 阻挡时停止，不绕过 |
| 本机官方 `global.ini` | 官方简体中文名称 | 只读本地解析，SHA-256 追踪 | 中文最高优先级 | 只提交派生词典，不提交整个数据目录 |

## 已核验的总体事实

- 维科洛是斯坦顿星系的巴努商人。
- 交易站共三座：Dasi（Hurston 附近）、Selo（Crusader 附近）、Kinga（microTech 附近）。
- 用户在集市接受合同，通过货运电梯上交物品；奖励在完成合同的站点本地库存中领取。
- Wiki 声望阶级：New Customer、Very Good Customer、Very Best Customer。
- 当前主结构化数据版本：`4.10.0 LIVE.12519617`，来源提取时间 `2026-08-27T07:09:49.582Z`。
- SC Market 当前公开游戏数据版本为 `4.9.0-LIVE.12232306`，最后更新时间 `2026-07-25T18:36:56.065Z`；它只能作为旧版快照参考，不能覆盖更新的 4.10 主结构化版本。
- 次级公开 patch 当前为 4.8.1，不能与 4.10.0 混用；当前 15 笔差异合同保留 `conflict` 状态。

这些总体站点事实不等于逐条合同地点。没有结构化绑定时必须保持 `station.status = missing`。

## 标准数据模型

完整 Schema：`data/schema/trades.schema.json`。

每条交易必须包含稳定 ID、中英文合同名、类别、精确游戏版本、有效状态、最低声望、声望增量、交易站对象、上交物数组、奖励数组、蓝图标记、来源 URL、来源更新时间、抓取时间、校验状态和冲突数组。

上交物与奖励必须包含内部 ID、中英文名、数量、单位、类别、蓝图标记和游戏绑定标记。允许单位仅为 `x`、`SCU`、`套`、`件`。多奖励和蓝图不得截断。

校验状态：

- `verified`：至少两个来源匹配；
- `single_source`：只有一个可用结构化来源；
- `conflict`：来源原始值有差异，全部保留；
- `pending`：待校验，不可发布。

## 图片与获取方式

`src/data/generated/items.json` 中：

- `imagePath` 指向站点内离线图片；
- `imageSourceUrl` 记录图片原始来源；
- `imageKind` 在数据层区分精确物品图 `exact`、基础型号参考图 `base_model`、玩家社区实拍 `community`、AI 重绘 `ai_redraw` 和无图 `none`；公开界面只对基础型号图显示“参考图”，不显示 AI 重绘角标、图片来源站点、来源链接或维护说明；
- `acquisition[]` 保留获取类型、标签、地点、价格、货币、来源 URL 与来源更新时间；
- `requiredBy[]` 和 `rewardedBy[]` 保留每个相关合同的精确数量和单位。

物品弹窗不得使用“作为上交物”这类生硬术语。统一显示“可用于哪些交易”，并解释“以下交易会消耗这个物品”。可制作物品按“先解锁制作蓝图 → 准备材料并开始制作 → 可用于哪些交易”呈现；材料必须显示官方中文主名、英文副名和数量，界面可把数据单位 `x` 友好显示为“个”，SCU 等专用单位保持原样。

当前并非所有 271 个条目都有精确图片。精确、清晰且画幅合适的真实游戏图仍为第一选择；真实素材能够确认具体物品，但因低清晰度、杂乱背景、构图或画幅不适合网站时，允许使用 Image 2 以该真实素材为参考进行高保真重绘。重绘只能调整清晰度、背景、构图和适配画幅，不得改变物品型号、颜色、结构、涂装或关键辨识特征；成图在离线数据中使用 `ai_redraw`，公开页面不额外显示重绘角标，且不得标为 `exact`。没有可靠真实参照时仍使用中性占位，禁止仅凭名称虚构外观。

AI 重绘资产必须在离线映射中记录参考页面、参考图片 URL、成图 SHA-256、生成产物标识、水印状态、重绘类型和说明。AI 重绘只解决网站视觉适配，不参与配方、数量、版本、地点、获取方式或物品属性的事实判断。社区实拍、临时参考图与 AI 重绘的维护信息不得作为多余正文显示在普通页面，公开页面只对基础型号参考图保留必要的“参考图”标识。

当前本地数据有 265 / 271 个条目使用本地图片；118 / 118 种上交材料有图，剩余 6 个无图条目全部是蓝图。29 个实体物品使用仅在离线数据中区分的 Image 2 重绘，其中 `Metamaterial Test #152`、Strata Arms、Strata Core 和 Strata Legs 已不再显示占位。王牌截击者头盔、草地故夕食草兽蛋、ADP-mk4 林地版胸甲/臂甲/腿甲/头盔，以及六排勋章、政府制图局勋章、ASD 安全驱动器、维科洛货币、Yormandi 部位、Vanduul 材料、Parallax、Quartz 和多数矿物货箱仍直接使用可核验精确实图。媒体域拒绝脚本请求时，只使用正常公开页面已经呈现的资源，禁止绕过 WAF。

新增核验：ATLS 冷铁使用 [RSI Community Hub 原帖](https://robertsspaceindustries.com/community-hub/post/atls-geo-cool-metal-edition-from-wikelo-rXU9GCHNUTdAa)中的明确同名特别版实拍；BUL-H4 护甲与头盔使用 [RSI Community Hub 的 BUL-H4 实拍](https://robertsspaceindustries.com/community-hub/post/bul-h4-2CdS7dybL4mlg)。Venture Core、Venture Legs 与 Vendetta HMG 固定到 Star Citizen Wiki 的对应文件，Jaclium (Ore) 固定到 SC Market 公共物品数据返回的无水印实物图。Star Citizen Wiki 的页面图接口曾把 Venture Arms 错误返回给 Venture Legs，现已绕开错误映射并删除本地误标旧文件。

2026-08-24 本地补图新增蓝月菌、萨达瑞晶、RCMBNT 合成样本系列和 Strata 护甲分件。蓝月菌与 RCMBNT 取自 RSI Community Hub 的可核验游戏内实拍，萨达瑞晶取自画面明确标注 `SADARYX NODES` 的游戏攻略截图。RCMBNT 的七个单独编号无法从合照逐一分辨，Strata 只找到同型号整套/涂装图，所以两组在数据层使用 `base_model`，公开界面必须显示“参考图”。

2026-08-26，项目所有者提供了画面文字明确标识 `IRRADIATED VALAKKAR FANG (APEX)` 的受辐射瓦拉卡巅峰期獠牙实图。映射记录使用 `sourceType: user_provided`，同时保存提供时间与 SHA-256，刷新时按 `exact` 精确实图保留。

H4-PBF Ammo Carrier、Metamaterial Test #146、基础 Monde 四个护甲分件、Saldynium (Ore) 和 Warden Backpack Monde 仍无精确物品图。2026-08-26 按项目所有者要求临时收录真实游戏参考图：H4 系列 RSI Community Hub 实拍、Reddit 玩家实际合成界面、CIG 官方 Monde Keystone 整套图、SC Market 玩家矿石货物图和 Star Citizen Wiki API/CStone 的 Warden 基础型号图。8 条记录均为 `base_model`，离线保存来源、SHA-256、`hasWatermark`、`temporaryReference` 与 `replacementPriority`；公开页面只显示“参考图”，不增加来源或水印文字，轮巡发现精确素材后替换。

118 / 118 种上交材料均有带来源的获取说明。说明区分固定任务、设施合成、蓝图制作、商店购买、采集/采矿、狩猎掉落与随机战利品；随机来源会明确写“没有保证出现的固定点”，不会把抓取缺失伪装成游戏机制。公开界面不显示内部类名、缓存文件名、抓取错误或调试标记。

用户可见获取说明统一使用游戏语义，例如“可从 4 笔兑换合同获得，具体合同与数量见下方”“没有已确认的固定掉落点”。`公开数据`、`结构化来源`、`来源合同`、`内部任务占位`、`交叉校验` 等维护措辞被内容审计列为禁止公开文本。

## 汉化规则

只读官方源：`/Users/bindox/Documents/data/localization/chinese_(simplified)/global.ini`。

禁止修改源文件。只提交项目实际需要的 `data/localization/official-global-derived.json` 与 `src/data/generated/localization.json`，不得提交整个本机数据目录。前者供无法访问本机路径的 GitHub Runner 只读回退，必须覆盖交易、上交物、奖励及全部制作配方材料；派生元数据必须包含 `sourcePath`、`sourceSha256`、`sourceUpdatedAt`、`generatedAt`。解析时只按第一个 `=` 分割；官方值中 `\n` 后的英文不应覆盖传入的英文原名。

解析器先按内部标识和已核验键别名查找，再按官方条目中的英文原名做精确匹配；不得用相似字符串猜测合同译名。当前生成数据中 271 / 271 个物品与 86 / 86 个合同均有可靠中文显示名；ASD 重组样本在官方文件中的正式名称本身就是 `RCMBNT-*` 代码。官方派生快照不再只保留旧版已引用条目，而是预载全部官方物品、商品、TheCollector 合同和蓝图名称；因此 4.10 新增的 `BUL-H4 护甲 雪地迷彩`、`BUL-H4 头盔 雪地迷彩`、`H4-PBF 弹药携具 雪地迷彩`、`超材料测试 #152` 均由官方 `global.ini` 直接命中。SCU 材料必须使用 `resourceName` 参与官方键查找，不能只查 `entityClass`。

舰队品牌与游戏汉化必须分开处理：Logo/页脚正式主品牌使用“星远”，指向官网的对外入口使用“星远舰队”；`data/localization/official-global-derived.json` 等游戏数据中的“星际远航者”是 `Starfarer` 舰船及其部件、涂装的官方中文，必须原样保留。禁止为了品牌同步对数据目录执行全局替换。

`ASD Power Module (PWL-2/PWL-3)` 与 `ASD Regulator Module (RGL-1)` 是上游交易数据使用的英文标签。官方简中 `global.ini` 中对应物品名只有代码 `RCMBNT-PWL-2`、`RCMBNT-PWL-3`、`RCMBNT-RGL-1`，没有正式中文名称；但官方中文说明确认它们分别是催化剂 PWL/RGL 与反应物 02/03/01 组合得到的“印记重组样本”。界面保留英文原名，并在材料弹窗中显示官方中文说明、准确组合与“约里特卷宗：海伯利安项目”获取路径，不得杜撰中文物品名。

无可靠中文时显示英文原名，不能创造译名。物品优先显示 `中文名` 与英文副标题；尺寸必须使用 S0/S1/S2。

## 更新、备份与故障回退

刷新命令：`npm run refresh:data`。

生成前会进行 Schema 校验和异常检测；全部通过后才备份并原子替换。备份保存 14 天，可用以下方式恢复：

```bash
npm run restore:data
npm run restore:data -- <备份目录名>
```

如果抓取、标准化、校验或异常检测失败，脚本退出并继续使用上一版稳定数据。不得把空数组或部分数据写入稳定文件。来源指纹未变化时必须显示 `changed: []` 和 `unchanged: true`。

项目内 `.github/workflows/refresh-data.yml` 在每 6 小时的第 23 分钟运行一次轻量检查，刻意避开 GitHub Actions 容易延迟或丢弃任务的整点高负载时段。轻量阶段只读取 Dumper's Repo 维科洛 HTML 页并提取精确 LIVE build，不下载完整 JS bundle；LIVE 未变化时工作流绿色结束，不运行构建、不写文件、不提交。只有出现新 LIVE，或维护者显式手动选择 `force_refresh`，才先 fetch/rebase，再执行完整抓取、内容审计、测试、Schema 校验和构建。远端汉化必须使用仓库内已验证的官方派生快照，缺失或覆盖不完整时直接失败，绝不能回退成英文后提交。只有语义数据确有变化且全部门禁通过才提交，并保留 14 天恢复工件。该 Action 只服务 EdgeOne 的仓库连接，不包含 GitHub Pages，也不依赖本机或 NAS。

独立的 `.github/workflows/refresh-watchdog.yml` 在 UTC 02:53、08:53、14:53、20:53 运行，位于每次主检查之后约 2 小时 30 分钟。它读取主工作流最近的运行状态；若没有排队/运行中的主任务，且最近成功检查已超过 8 小时，就通过 `workflow_dispatch` 自动补发轻量检查。仓库为公开仓库，GitHub 可能在 60 天无仓库活动后停用定时工作流；因此看门狗最多每 30 天在隔离的 `automation-heartbeat` 分支更新一次 `.automation/heartbeat.txt`。该分支不属于 EdgeOne 生产分支，不改变 `main`、稳定数据或正式站，也不会因心跳重做正式站。主检查、看门狗补发和人工补跑共用同一并发锁与发布门禁，任何重复运行都不能绕过 LIVE-only、语义差异、汉化覆盖、内容审计、测试及构建验证。

版本解析现在只接受 Dumper's Repo 维科洛页的精确 LIVE build；PTU/EPTU 会被数据层拒绝，不进入 `src/data/generated/versioned-data.json`。页面只使用一个 LIVE 快照，不提供版本筛选。刷新器按稳定 ID 比较交易与物品，输出新增、修改、删除数量；语义指纹忽略抓取时间、来源时间、数组顺序和二次数据源的小版本噪声，避免假变化触发提交。即使语义指纹未变，刷新器也会校验 `versioned-data.json` 是否与稳定的 `items.json`、`trades.json` 完全同步；发现旧快照时先备份再只修复版本快照。新 LIVE 仍必须完整通过交易 Schema、数量/版本异常、来源状态、汉化覆盖率及构建门禁，失败时保留上一版。

## 测试与浏览器检查

基础门禁：

```bash
npm test
npm run audit:data
npm run lint:data
npm run build
```

固定 fixture 覆盖：远端静态字面量解析、SCU/x、多个奖励、蓝图、来源冲突、仅 LIVE 版本提取/倒退、异常回退基础逻辑、BOM/注释/等号汉化解析、官方译名优先、官方代码保留，以及卡片全部材料、精确图片、当前全部材料获取说明、制作步骤、SC Market 二次版本源、交易详情和备案页脚交互。独立回归覆盖 PTU/EPTU 拒绝、单一 LIVE 替换、相同 LIVE 纠错、旧版倒退阻断、语义指纹防抖、版本徽标更新时间及版本筛选彻底移除。生成数据测试不得硬编码某一版的交易数、材料数、固定合同用途数或旧版次级来源状态，必须从当前元数据和稳定实体动态计算预期值。

浏览器审查必须覆盖 2560×1440、1920×1080、1440×900、768×1024、390×844、320×568，以及 80%、100%、125% 缩放。需要检查横向溢出、遮挡、筛选菜单方向/内部滚动/键盘、空状态、手机交易详情弹窗、材料获取弹窗、控制台和失败网络请求。

### 2026-08-18 发布审查：蓝图站同款视觉主题与图片补全

- 改版目标仅是统一可见风格，不统一内部实现。蓝图站仍为只读参考，未修改 `gvy-lantu-site`。
- 新增 `src/blueprint-theme.css` 并在 `src/main.tsx` 中最后加载；该文件集中覆盖设计令牌、页面壳体、页头、搜索/版本/站点按钮、类别、筛选、交易卡、详情与物品弹窗、图片放大层、页脚和断点规则。
- 维科洛的 `App.tsx` 只把搜索清空按钮从图标改为与蓝图站一致的文字按钮；数据结构、筛选条件、材料获取方式和弹窗流程未与蓝图站耦合。本次另独立修复图片数据与版本快照同步，不改变交易配方。
- 改版前完整项目备份：`/Users/bindox/Documents/Codex/Backups/gvy-wikelo-site-before-lantu-style-20260818-180500.tar.gz`；SHA-256：`bd01e8391200b54a76914132d8f66dfa6d51efad82dae76d1e13f0b95a817bd7`；归档已通过 `tar -tzf` 可读性校验。
- 自动化门禁：`npm test` 为 9 个测试文件、58 项全部通过；`npm run audit:data`、`npm run lint:data` 和 `npm run build` 通过。内容审计确认 87 笔交易、268 个物品、119 / 119 种上交材料获取说明完整；只保留 21 项缺图警告。Vite 仍只报告既有的单 JS chunk 大于 500 kB 提示，不是本次主题回归或运行错误。
- 浏览器验收：2560×1440、2400×1350（1920 宽 80% 等效）、1920×1080、1536×864（1920 宽 125% 等效）、1440×900、768×1024、390×844、320×568 的 CSS 视口均无页面级横向溢出；3 / 2 / 1 列按断点重排。
- 交互验收：320px 下拉菜单与触发框左对齐、完整位于视口内，键盘可从“全部声望”选择到“无要求”；搜索“天蝎座”从 87 笔收敛为 1 笔，清空恢复 87 笔；交易详情、材料详情和仅详情内图片放大均可打开和关闭。
- 图片比例验收：飞船、地面载具、武器等宽幅内容与护甲、普通物品分类展示；640×462 的 Ursa 地面载具图在详情中使用 300×169 的 16:9 宽幅框，`object-fit: contain`，不裁切主体，也不再被归为正方形。
- 运行健康：本地预览标题与 URL 正确，无 Vite/框架错误覆盖层，控制台无 error/warning，已加载图片无损坏；DOM 对外链接仍仅有舰队官网、蓝图站、ICP备案和公安备案四个白名单链接。
- 用户已验收整体改版并明确要求审查后部署。本次实现已由提交 `718240c` 推送到 `main`，EdgeOne 已完成自动发布，正式域名返回新构建资源 `assets/index-BOG30V_M.js` 与 `assets/index-uXmuYtuy.css`。
- 生产复核：HTTP 301、HTTPS 200；87 笔交易和 `4.9.0 LIVE` 正常；390×844 交易详情在网页可用视口内垂直居中且无横向溢出；Ursa 详情图片为 `media-landscape` 宽幅框，原图 640×462、`object-fit: contain`；“冒险探险家”详情中的 Venture Core、Venture Legs 与 Jaclium 新图片均加载成功；控制台 0 error / 0 warning；DOM 外链仍只有舰队官网、蓝图站、ICP备案和公安备案四项。

2026-08-19 第一轮页脚修复保持了蓝图站原有的三段式 `space-between` 横向布局、备案区自动左外边距和页脚左右宽度不变，仅移除 `main` 的 16px 底部内边距。正式发布提交为 `af3064a`，资源为 `assets/index-DIUttttt.css` 与 `assets/index-C6EXUMSh.js`。该轮检查误把 `main` 底部与页脚底部的距离当成站壳底部距离；用户发布后截图证明，在 2560×1440 且筛选“维科洛人情”时，页脚底部到 `.app-shell` 底部仍有 232px 空白，不能视为已解决。

2026-08-19 第二轮按蓝图站的自适应内容面板逻辑修复根因：`.app-shell` 改为纵向弹性容器，`main` 纵向伸展，`.results-section` 自动补足视口剩余高度，使页脚随站壳贴底，同时保留结果区与页脚之间原有的 16px 间距和页脚自身尺寸。正式发布提交为 `586ca7b`，EdgeOne 资源为 `assets/index-BgPG9Ork.css` 与 `assets/index-GSpIVLQW.js`。生产环境“维科洛人情”5 笔结果在 2560×1440、1920×1080、1620×1254 和 390×844 下，页脚底部到站壳底部均只剩 1px 边框；无横向溢出，备案完整，控制台 0 error / 0 warning。完整 87 笔列表也保持自然增长和滚动；数据发布门禁确认 87 笔交易、4.9.0 LIVE、无异常且无数据文件变化。

2026-08-16 修复了手机端交易详情偏下的问题：`max-width: 840px` 断点中的 `.trade-modal` 不再使用 `align-self: flex-end` 贴底，而改为 `align-self: center`。本次只修改这一项定位规则，未改变弹窗内容、尺寸或桌面端样式。使用 390×753（对应用户截图中扣除原生浏览器顶栏后的网页可用区域）验证时，弹窗上下留白均为 109.5px、中心偏差为 0、无横向溢出；53 项 Vitest 和生产构建通过。正式发布提交为 `ce089ee`。

### 2026-08-27 4.10 LIVE 与无人值守链路修复

- 根因不是抓取器不会识别 4.10：Dumper's Repo 在 2026-08-27 10:10（中国标准时间）后才发布 `4.10.0-live.12519617`；同时旧主任务使用整点 cron，GitHub Actions 的计划任务在高负载时可能延迟或被丢弃，导致应有检查没有按时出现。
- 主任务改为每 6 小时第 23 分钟轻量检查；独立看门狗在每轮主任务约 2 小时 30 分钟后检查心跳，最近成功主检查超过 8 小时且没有活动实例时自动补发。最多每 30 天在隔离的 `automation-heartbeat` 分支写入维护心跳，避免公开仓库长期无活动后计划任务被平台停用；该分支不触碰生产分支 `main`。
- 4.10 第一次真实刷新被内容审计拦截，因为 7 个新增实体尚未进入远端官方派生汉化快照；修复后快照预载官方物品、商品、TheCollector 合同与蓝图名称。第二次被旧版硬编码测试拦截；测试改为从当前生成数据动态计算预期。两次失败均未覆盖 4.9 稳定数据，证明发布门禁有效。
- 第三次真实刷新 GitHub Actions `33048577364` 全部通过：86 笔交易、271 个物品、118 种上交材料；86 / 86 合同、271 / 271 物品、258 个制作材料完成汉化，118 / 118 上交材料有获取说明。语义差异为合同修改 4、删除 1，物品新增 4、修改 21、删除 1；自动数据提交为 `86efca0`。
- 看门狗第一次手动验收暴露缺失分支的 GitHub API 404 响应被误当日期的问题；`2f71662` 改为按命令退出状态识别不存在的分支/文件。第二次实跑 `33048848626` 成功，确认主刷新健康时不重复抓取，同时创建隔离心跳分支且 `main` 不变。
- 正式域名返回新资源 `assets/index-C8jcairz.js`，页面显示 `4.10.0 LIVE` 与 86 笔交易。搜索 `超材料测试 #152` 能定位到“快船现在主打战斗”，详情正确展示蓝图解锁、0.5 SCU 稀钛铁、0.3 SCU 欧拉特烃、10 秒制作时间和用途。390×844 下 `scrollWidth = clientWidth = 390`，控制台无 error/warning。

### 2026-08-28 Image 2 重绘补图

- 对没有独立实图或现有图片不适合正式展示的实体物品进行 Image 2 重绘，当前保留 29 条物品映射、复用 23 张本地高质量 WebP；原始生成文件保留在 Codex 生成目录，站点只收录压缩后的成品。
- 每条映射在 `data/source-snapshots/ai-redraw-item-images.json` 内保存参考页面、参考图片、重绘类型和说明；刷新管线最后加载这组映射，确保每 6 小时检查和未来 LIVE 更新不会把人工核验后的重绘退回旧参考图。
- 公开界面不再显示“AI重绘”角标，也不显示来源链接或维护文字。29 个重绘物品均有本地文件和 HTTPS 参考来源；剩余 6 个 `none` 全部是蓝图记录，蓝图不虚构独立实体图片。
- BUL-H4 护甲雪地迷彩的首版重绘曾错误生成为无头普通重甲，后续修正版仍与真实游戏造型存在偏差。2026-08-28 项目所有者提供了“沉重又明亮”合同的全身与近景游戏截图，已将 BUL-H4 护甲、头盔和 H4-PBF 弹药携具雪地迷彩全部改为 `user_provided` 精确实图，并删除三张旧重绘及其覆盖映射。任务标题已再次对照本机官方 `global.ini`：`TheCollector_Recipes_Title_SuperHeavyCombat=沉重又明亮`，当前中文名正确。
- MISC Starlancer TAC Wikelo War Special 使用 Star Citizen Wiki 的准确 `Starlancer TAC Wikelo.png`（1048×512）重新清晰化；早期生成的错误圆形舰体未采用。Metamaterial Test #146、#152 与 Vendetta 弹匣因公开资料没有独立物品实图，明确记录为概念重建，不冒充官方实拍。
- 本地 4.10 LIVE 数据审计结果：118 / 118 种上交物有图片及获取方式，数据校验与生产构建通过。2560×1440 和 390×844 验收时图片保持原比例 `contain`、无页面横向溢出、控制台无 error/warning，详情图放大与关闭交互正常。

### 2026-08-30 卡片重复版本号清理

- 交易卡右上角的版本文字源自建站首版提交 `7bc4c0e`，不是 2026-08-28 图片修改新增；2026-08-18 统一蓝图站视觉后，其位置和留白让这项重复信息更明显。
- `src/App.tsx` 已从交易卡 DOM 中移除 `formatVersion(trade.gameVersion)`，并清理两份主题样式内不再使用的卡片版本文字规则。搜索框旁仍保留唯一的全站 LIVE 版本提示，交易详情中的“适用版本”继续保留。
- 新增回归断言，确保所有交易卡不再显示 `x.y.z LIVE`。59 项测试和生产构建通过；桌面端 86 张卡片的版本节点为 0、页头版本节点为 1，390×844 下同样为 0 / 1，页面无横向溢出且控制台无 error/warning。本次修复通过完整发布门禁后随维护提交部署。

### 2026-09-01 星远舰队品牌同步

- 舰队正式名称调整为“星远”，维科洛站面向访客的中文品牌统一为“星远舰队”；英文 `GVY / GALACTIC VOYAGERS`、域名和标志图案保持不变。
- 页头官网入口与页脚品牌已同步，相关外链白名单测试改为锁定“星远舰队”；本次不改合同、物品、图片、汉化、筛选或无人值守刷新逻辑。
- 发布前备份标签为 `backup-production-before-xingyuan-rename-20260901`。永久防线是只修改 `App.tsx` 的品牌位置；游戏派生汉化中的“星际远航者”代表 `Starfarer`，不得替换。

### 2026-09-04 名称语义校正（已部署）

- 上一轮把“所有公开位置”都概括为“星远舰队”，导致页脚 Logo 锁定区也错用口语称呼。正确规则是：页脚 Logo 正式名用“星远”，页头指向主站的对外入口用“星远舰队”。
- 修改基线为 `d4e20ecc6a389843f47a11474c94526038f39e8b`，正式功能提交为 `928258bc4d2b590d72bb91d5c4c19debf106e91c`；发布前远端回退标签 `backup-production-before-xingyuan-semantics-20260904` 指向该基线。只修改 `src/App.tsx`、对应测试、README 和本文档，没有修改交易、物品、图片、汉化、搜索、筛选、刷新或部署逻辑。
- 本地门禁已通过：63 项测试、内容审计（86 笔交易、271 个物品、118/118 上交物图片、118/118 获取说明）、LIVE-only 数据检查、生产构建和 Browser 桌面/390×844 实测全部通过；搜索“超材料测试 #152”精确返回 1 笔“快帆现在主打战斗”，无横向溢出。
- 已推送 `main` 并由 EdgeOne 发布；正式 Browser 显示页脚“星远”、官网入口“星远舰队”，无横向溢出或控制台 warning/error。以后测试必须同时锁定这两个语义场景，禁止再用“公开位置全部统一”的单一文案规则。

### 2026-09-01 同 LIVE 物品刷新审计与幂等修复

- 在干净的 `fb8c3deb878b1b135f64c61586f1abcc06b4cad7` 基线上独立复现：上游仍为 `4.10.0 LIVE.12519617`，原候选指纹为 `911c2e85091b115f81687f9d70437e14c544498d26464d5e26e623d0c65aa872`，86 笔交易不变，40 个物品被判定为修改。原始 40 项涉及 30 项 `acquisition`、9 项 `descriptionZh` 和 7 项 `imageSourceUrl`，字段存在重叠。
- 9 项说明是有效更新：BUL-H4 护甲、BUL-H4 头盔、H4-PBF 弹药携具、世仇雪地迷彩重机枪、世仇重机枪、世仇弹匣，以及探险家升天版臂甲、胸甲、腿甲。后三项同时纠正了误套用恋慕版文案和旧储物值；所有物品中文名仍来自本机官方 `global.ini`，交易、本地化词典、需求、奖励、任务来源、图片路径和图片类型均未变化。
- 30 项原始获取方式变化为：ADP-mk4 林地版臂甲/胸甲/头盔/腿甲、南船座 ATLS、浆果冰沙、爆破筒火箭、终曲弹匣、无脂冰淇淋、菲涅尔电池、草地故夕食草兽蛋、卡纳步枪/电池、绝杀弹匣、诺维科夫探索服/头盔、视差/棱镜/石英电池、R97 霰弹枪/弹匣、S71 弹匣、顺风头盔/飞行服、三重击弹匣、世仇重机枪/弹匣、探险家基础胸甲/腿甲、天顶电池。逐项对照当前 API 后确认其中包含真实新增/删除商店与价格变化，也包含旧实现“先截前 8 条再去重”造成的排序和截断波动。
- 7 项图片来源变化全部只是 `media.starcitizen.tools` 缓存参数：终曲弹匣、卡纳电池、诺维科夫升天头盔、S71 弹匣、三重击手枪、探险家升天胸甲、天顶步枪。刷新器现在只对该媒体域名移除查询参数和片段，不改页面 URL；诺维科夫升天头盔、天顶步枪两项纯噪声因此从候选消失，其余五项仍因获取方式或说明的有效变化保留。
- 获取地点现在先汉化、全量去重和确定性排序，再展示前 8 项并附总地点数；因此修正后的候选为 38 项（31 项获取方式、9 项说明，世仇重机枪及弹匣两项重叠），指纹为 `adc70aa68c8c05095bee8c374819569ad1ef4695aa1946fa4efbcf45fb14384e`。三重击手枪虽然原候选只有图片参数噪声，但固定后会进行一次确定性地点排序，此后保持稳定。
- 幂等根因有两处：首次仅物品变化时，版本快照错误采用了本轮来源状态而稳定交易文档仍保留旧来源状态，导致第二轮继续补写 `versioned-data.json`；同时任何真实物品变化都会把其他未变物品的新来源时间戳一并写入。现在版本快照始终使用实际持久化交易文档，且语义未变的物品直接复用稳定对象。连续两次使用相同上游执行发布检查，第二次返回 `changed: []`，五个生成文件哈希完全一致。
- 发布前回退标签为 `backup-production-before-wikelo-refresh-audit-20260901`。完整门禁结果：内容审计通过（86 笔交易、271 个物品、118 / 118 上交物图片、118 / 118 获取方式）、LIVE-only Schema 通过、63 项测试通过、生产构建通过；远端轻量检查仍为同一 LIVE，无 PTU/EPTU 数据进入生成文件。

## 部署与发布门禁

用户已明确批准正式部署。每次发布仍必须按顺序执行：

1. 再次核对 README 和本文档；
2. `git fetch` 并检查远端更新；
3. 整合远端，不覆盖自动刷新产生的新数据；
4. `npm run refresh:data:publish-check`、测试、构建；
5. 仅在本项目内 commit 和 push；
6. 通过 EdgeOne Pages / Makers 发布；
7. 检查正式域名、版本、汉化、导航、控制台、移动端布局；
8. 保持工作区干净。

不要配置 GitHub Pages，不要修改其他项目 DNS。

### 当前生产环境（2026-08-27）

- 正式域名：`https://wikelo.gvyvoyagers.vip`
- GitHub：`B1ndoX/gvy-wikelo-site`，生产分支 `main`
- EdgeOne Makers：`gvy-wikelo-site`，安装命令 `npm install --ignore-scripts`，构建命令 `npm run build`，输出目录 `dist`
- 运行时：`edgeone.json` 固定 Node.js `22.21.1`；项目最低要求为 Node.js `>=22.12.0`
- DNS：`wikelo` CNAME → `wikelo.gvyvoyagers.vip.pages.dnsoe4.com`，TTL 600；未改动其他项目记录
- HTTPS：免费 RSA 2048 证书已部署并设置到期前 15 天自动更新；HTTP → HTTPS 使用 301
- 安全开关：HSTS `max-age=31536000`、不包含子域名、不启用 preload；OCSP 装订开启
- 响应头：`X-Content-Type-Options: nosniff`、`Referrer-Policy: strict-origin-when-cross-origin`、`X-Frame-Options: SAMEORIGIN`
- 正式域名验收：HTTP 301、HTTPS 200；86 笔交易、`4.10.0 LIVE`、官方中文、备案页脚、材料检索与获取方式弹窗正常；390×844 无页面级横向溢出；控制台无 error/warning
- 响应式验收：2560、1920、1536、1440、1024、768、390、320 CSS 视口均无页面级横向溢出；1920 宽度下的 80%/125% 缩放等效宽度 2400/1536 也正常重排
- 外链验收：页面 DOM 仅存在舰队官网、蓝图站、ICP 备案和公安备案四个链接
- GitHub Pages 保持未启用。每 6 小时轻量检查不触发部署；只有新 LIVE 的已验证真实数据提交才触发 EdgeOne 仓库部署。
- 当前 4.10 数据提交：`86efca0`；官方未来条目汉化快照修复：`f98721e`；跨版本门禁修复：`bb5e1d6`；无人值守主任务与看门狗：`e5f8433`、`2f71662`。视觉基线继续使用 `718240c`、`586ca7b` 和 `ce089ee` 的蓝图站同款主题、页脚自适应与手机弹窗定位。
- GitHub Actions `33048577364` 已验证新 LIVE 完整路径；`33048848626` 已验证看门狗健康路径和隔离分支保活。后续仍应定期查看 Action 是否成功，但正常运行不依赖本机或人工在线。

首次成功的 EdgeOne 正式部署对应提交 `807d33c`。后续每次更新仍须先 fetch/rebase、通过全部门禁，再由 `main` 推送触发 EdgeOne；不得直接上传陈旧的本地产物覆盖仓库数据。
